export const CELCIUS = "°C";

export const trunc = (number) => {
  return Math.trunc(number);
};

export const round1dec = (number) => {
  return parseFloat(number.toFixed(1));
};

export const round = (number) => {
  return Math.round(number);
};
